/**
 * Reading and selecting RS3 dialog options.
 *
 * <p>{@link com.botwithus.bot.api.dialog.Dialog} models the multi-choice
 * ("select an option") dialog: it correlates each visible option label with the
 * option-row component the server keys the click on, so callers select by text
 * or index without knowing the row component ids.</p>
 */
package com.botwithus.bot.api.dialog;
